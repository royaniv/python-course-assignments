#expontential growth calculations using a fucntion and input

from expgrowfunct import expgrowth

N0 = float(input("initial amount (N0): "))
r = float(input("growth rate (r): "))
t = float(input("time (t): "))
k = float(input("decay rate (k): "))
    N = N0 * math.exp((r - k) * t)
    return N

if __name__ == "__main__":    final_amount = expgrowth()
print(f"Final amount (N) after time t: {final_amount}")